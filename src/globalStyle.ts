import { StyleSheet } from 'react-native';

export const globalStyle = StyleSheet.create({
  textInput: {
    fontSize: 18,
    color: '#fff',
    borderWidth: 1.5,
    borderRadius: 7,
    width: '90%',
    borderColor: "#404040",
    padding: 7,
  }
});
import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#fff'
  },
  row: { flexDirection: 'row' },
  textInfoView: { flex: 1, justifyContent: 'space-evenly' },
  tagsView: { flex: 1, alignItems: "center" }
});